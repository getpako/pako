services:
  tuf:
    image: nginx:alpine
    ports:
      - "127.0.0.1:8080:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - ../.dev/tuf/metadata:/srv/tuf/metadata:ro
      - ../.dev/tuf/targets:/srv/tuf/targets:ro
