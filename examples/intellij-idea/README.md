# IntelliJ IDEA external fixture

This is a schema and validation fixture for `external-archive`. It is not a
production IntelliJ package: the loopback URL and one-byte checksum are test
values for a local HTTP fixture. Replace them only after independently
verifying a real upstream release, license, size, checksum, and transforms.
